You are helping me fix and extend company and user logos / avatars in my Innovatr portal project on Replit.

Right now:

I have uploaded logo images for Innovatr, Rugani Juice and Greenway Farms, but they are not appearing in the admin users table or portal header.

In the Admin → Users panel, every user still shows the default initials circle.

I would like company logos to show wherever possible, and later let users upload their own profile pictures.

Please inspect the existing code and then implement the following behaviour without breaking anything else.

1. File storage and paths

Create or confirm a folder for company logos:

/public/assets/logos/

Assume I will upload the logos there with these filenames (do NOT rename in code):

/public/assets/logos/logo-innovatr.png

/public/assets/logos/logo-rugani.png

/public/assets/logos/logo-greenway.png

Create or confirm a folder for user profile avatars:

/public/assets/avatars/

For now you only need to support uploaded files from the portal forms. No external APIs, no Google lookups.

2. Data model updates

Check how users and companies are currently modelled (it might be a simple in memory store, JSON file or a small database). Extend the models in the lightest possible way:

Company model

Add an optional logoUrl string field that stores the relative path to the company logo, for example /public/assets/logos/logo-rugani.png.

User model

Add an optional avatarUrl string field for a custom profile picture.

Confirm that each user already has a companyId or similar. If not, keep the current link between user and company as is and do not refactor the whole system.

Seed or update the demo data so that:

The Rugani Juice company has logoUrl set to /public/assets/logos/logo-rugani.png.

The Greenway Farms company has logoUrl set to /public/assets/logos/logo-greenway.png.

The global Innovatr brand uses /public/assets/logos/logo-innovatr.png wherever needed.

Do not hard code these paths in random components. Store them on the company objects.

3. Avatar / logo display logic

Create a single reusable <Avatar /> component (or update the existing one if it already exists) and use it everywhere the user circle currently appears (admin users table, portal drawer header, etc).

The component must follow this logic, in this order:

If the user has avatarUrl, display that image in a rounded avatar.

Else if the user belongs to a company with logoUrl, display the company logo.

Else if the user is an Innovatr team member
(either role is admin OR their email ends with @innovatr.co.za),
display the Innovatr logo.

Else fall back to the existing initials based avatar.

Make sure:

Avatars look good at small sizes in the Admin → Users table.

The component works in both light and dark mode.

You do not break any layouts or tailwind classes around the existing avatar slots.

4. Admin Users panel and portal header

Admin → Users

Replace the current initials circle with the new <Avatar /> component.

For the four Rugani / Greenway users that already exist, their rows should now show the Rugani or Greenway logo via the company they belong to (do not hard code by email, use the company mapping).

Portal header / left drawer

When a user belongs to a company (Rugani Juice or Greenway Farms), show that company logo in the portal header / drawer instead of the default initials.

When a user is an admin or has an @innovatr.co.za email, show the Innovatr logo.

Keep the rest of the header UI exactly the same (name, tier badge, etc).

5. Optional avatar upload on signup or profile

If the project already has a signup or profile form for users:

Add an optional file upload field called Profile image.

When a file is uploaded, store it under /public/assets/avatars/ and save the relative path on user.avatarUrl.

Use the new <Avatar /> component to display the chosen image after signup or on the profile page.

If there is no clean place to add this yet, skip the upload field and just leave a clear TODO comment in the relevant form file indicating where it should go in future.

Do not add any Google or external avatar lookups for now.

6. Do not break light/dark mode

The Trends & Insights page and the rest of the portal now support both light and dark mode. Make sure:

The new avatar images and logo placements respect the current background and text colours.

You do not re introduce the previous light/dark mode bug on the Trends & Insights page.

7. Summary for me

When you are done, reply in the Replit task panel with:

A short list of files you created or edited.

The exact prop interface of the <Avatar /> component.

Confirmation that:

Rugani and Greenway users now show their company logos in Admin → Users

Admin or Innovatr users show the Innovatr logo

Other users still see the initials avatar

Any places where I still need to manually upload images or adjust company data.

Do not change any of my existing report content, mailers, or scheduling logic.