// TODO: HeatmapOverlay component deferred — requires concept image assets from Upsiide
// that aren't available yet. When assets are ready, this component should render a
// concept image with a semi-transparent heatmap overlay showing attention zones using
// the data from the useDigHeatmap hook (zones array with x, y, radius, intensity).

export default function HeatmapOverlay() {
  return null;
}
